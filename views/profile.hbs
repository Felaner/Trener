<div class="container">
    <div class="row">
        <div class="col-12">
                <div>
                    <h3>{{course.name}}</h3>
                    <p>{{course.description}}</p>
                    <p>{{course.duration}}</p>
                </div>
        </div>
    </div>
</div>